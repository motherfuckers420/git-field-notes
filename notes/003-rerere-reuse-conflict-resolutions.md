# Let git remember how you solved a conflict

rerere records conflict resolutions and replays them. Invaluable during a long rebase you have to restart.

```bash
git config --global rerere.enabled true
```
