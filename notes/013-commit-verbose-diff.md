# Write better messages with a diff in the editor

--verbose puts the staged diff below the message template so you describe what you actually staged.

```bash
git config --global commit.verbose true
```
