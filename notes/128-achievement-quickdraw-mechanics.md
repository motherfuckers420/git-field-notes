# Quickdraw is measured from creation to close

The five-minute window starts when the issue or PR is created, so a fast comment does not help.

```bash
gh issue create --title t --body b && gh issue close <n>
```
