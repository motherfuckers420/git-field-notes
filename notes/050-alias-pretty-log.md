# A log format you will actually read

A one-line graph with relative dates covers most of what you open a GUI for.

```bash
git config --global alias.lg "log --graph --oneline --decorate --date=relative"
```
