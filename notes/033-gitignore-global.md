# A global gitignore for editor cruft

Per-repo ignores should describe the project. Your editor's files are your problem, not the repo's.

```bash
git config --global core.excludesfile ~/.gitignore_global
```
